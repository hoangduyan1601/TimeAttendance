server:
  port: 9090
spring:
  application:
    name: core-api
  datasource:
    url: jdbc:postgresql://127.0.0.1:5433/smartops_db
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true # Chạy data.sql SAU khi tạo bảng
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  sql:
    init:
      mode: always
      encoding: UTF-8
 # Luôn chạy script khởi tạo
  jackson:
    time-zone: Asia/Ho_Chi_Minh
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: 20223457@eaut.edu.vn # Email dùng để gửi đi
    password: "@Aq16012004" # Mật khẩu ứng dụng Gmail
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  expiration: 86400000 # 24 hours in milliseconds

admin:
  email: hoangduyankt@gmail.com # Email nhận thông báo

upload:
  path: uploads/ekyc
ai-service:
  url: http://localhost:8080 # URL của Python FastAPI
  extract-endpoint: /internal/ai/embed
  compare-endpoint: /internal/ai/compare
